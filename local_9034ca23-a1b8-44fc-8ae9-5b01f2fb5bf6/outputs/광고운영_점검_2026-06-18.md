# 폰스팟 광고운영 점검 — 2026-06-18

출처: Drive B1 스냅샷(`PhoneSpot Sheet Snapshots`, 2026-06-18 03:09 KST 생성) + `동기화_로그` 탭 + `GA4_자동` 탭.
방법: 스냅샷 JSON read만 사용(시트 직접 수정 안 함). `script.google.com` 차단으로 web app 직접 호출 불가 → Drive MCP 우회(SYSTEM_MAP G단원 B1 규칙).

---

## 1. 동기화 상태 (06-18 새벽 트리거, 로그 전부 ✅)

| 채널 | 최신 데이터 | 로그 | 판정 |
|---|---|---|---|
| 메타 | **06-17** | syncMetaDaily 01:35 → 인사이트 01:38(27광고/10그룹) → 소재 200 → autoDiscover 신규2 → 통합 7그룹 | 정상. GA4세션>0 **105행**, 카톡클릭 1,092 |
| 인스타 | 06-16 | 02:00/02:05 갱신7 | 정상 (46행) |
| 당근 | **06-16**(수기) | syncDanggnGA4 02:34 ok | 정상. API 없음 = 06-17/18 수기 미입력(동기화 실패 아님) |
| 네이버 | **06-15** | syncNaverIntegrated 02:14 **"OK (0 광고그룹)"** | 동기화 성공이나 광고그룹 0개 반환 = **집행 중단/비활성 추정** |

## 2. KPI · 매트릭스 (전부 라이브 수식 `=TODAY()`/`SUMIFS` → 항상 최신)

- 이번달 광고비 1,587,532 · 어제 52,366 · 7일 513,198 · 14일 1,189,129 · 30일 2,632,966
- 매트릭스 어제(06-17): 메타 노출 6,268 / 네이버·당근·구글·카카오 0(해당일 데이터 없음)
- "마지막 전체 새로고침 **2026-06-11 16:53**" = refreshAll 수동 마지막 시각일 뿐. KPI는 라이브라 정확도 영향 없음. 메타 인사이트도 야간 트리거로 매일 갱신됨 → **전체 새로고침은 정확도상 불필요**(원하면 시트 메뉴 🚀).

## 3. 광고그룹 추이

이미 셋업 + 데이터 존재(채널:메타 / 그룹:리틀리_가격확인_260417 / 기간:7일 / 데이터 06-01~06-15).
06-16·17 반영하려면 시트 메뉴 **📊 광고그룹 추이 → 🔄 추이 갱신**.

---

## 4. ★ 발견 이슈 A — 네이버_통합 GA4 매칭 전 행 0 (코드 패치 완료, push 대기)

- **증상**: 네이버_통합 317개 전 행 GA4세션·카톡·문의 = 0.
- **원인**: 수식이 **삭제된 옛 `네이버_UTM_매핑` 시트**를 `VLOOKUP(E,'네이버_UTM_매핑'!A:B,2)` 참조 → 에러폴백 → 매칭 0. (메타는 통합 `FILTER('UTM_매핑',채널="페북")`로 정상.)
- **왜 안 고쳐졌나**: 코드(`naver-synce.js:412`)는 06-16에 통합 수식으로 이미 수정됨. 그러나 `syncNaverIntegrated`가 "0 광고그룹" 반환 → 기존 행 수식을 건드리지 않음 → 옛 수식 잔존.
- **패치(작성·검증 완료)**: `apps_script/naver-synce.js`에 `refreshNaverGA4AllRows()` 추가 — 데이터 전 행 K~S(11~19) 수식을 통합 UTM_매핑 기준으로 재작성(당근 '🔄 GA4 매칭 새로고침'과 동일 개념). 메뉴 `🟢 네이버 자동화 → 🔄 GA4 수식 전체 재작성 (매핑 복구)` 추가. `node --check` PASS.
- **사장님 실행**: (1) 아래 git push → Actions 배포 → (2) 시트에서 메뉴 클릭 1회.

## 5. ★ 발견 이슈 B — 네이버 슬러그 불일치 (수식 패치로도 안 풀림)

GA4_자동 실측 네이버 utm_campaign vs UTM_매핑 C열:

| UTM_매핑 매핑값 | GA4 실측값 | 매칭? |
|---|---|---|
| galaxy / phonespot / iphone / old_model / holyland / kids_phone / senior_phone | 동일 존재 | ✅ (패치 후 매칭됨) |
| **region** | GA4엔 **region_keyword**(17) | ❌ 불일치 |
| **mobile_carrier** | GA4엔 없음 | ❌ |
| (매핑 없음) | **powerlink**(122, 최다) | ❌ 어느 그룹에도 미연결 |

→ region 광고그룹은 UTM_매핑 C열을 `region`→`region_keyword`로 바꿔야 매칭. powerlink(최다 트래픽)는 어느 광고그룹 utm인지 사장님 확인 필요.

---

## 6. 미매핑 UTM 정리 (UTM_매핑 시트)

### (A) utm 값 결정 필요 — GA4 실측 기반 추천 초안

| 채널 | 광고그룹명 | 추천 utm_campaign | 근거(GA4 실측) | 확신도 |
|---|---|---|---|---|
| 당근 | 소식_가격확인_아이폰 | **a17** | 당근 GA4값 a17(22세션)=아이폰17, 갤럭시=price_check 대응 | 중~높음 |
| 당근 | 소식_기종가격확인(15행) | **price_check** | 당근 GA4 최다값 price_check(132), 규모도 최다 그룹과 일치 | 중 |
| 당근 | 리틀리_가격확인_260610 | (확인 필요) | 당근→리틀리 링크트리 경유 → GA4 source=`littly`(331)에 잡힐 수 있음 | 낮음 |
| 메타 | 리틀리_영상_직접촬영 | (신규 지정) | 메타 GA4 미매핑값=`fixed_price`(8)뿐. 기존 영상은 전부 1sec | 낮음 |
| 메타 | 리틀리_영상_AI(타이포) | (신규 지정) | 둘 다 1sec면 GA4서 A/B 구분 불가 → 새 슬러그 권장 | 낮음 |

→ 전부 **사장님이 실제 광고 URL의 utm_campaign과 대조 후 확정**. 추측 아님 = GA4에 실제 존재하는 값만 제시.
→ 별도: 메타 `fixed_price`(GA4 8세션)는 현재 어느 광고그룹에도 미매핑 = 확인 권장.

### (B) 상태만 ⚠️→✅ (utm 이미 정확, 라벨만 갱신)

`리틀리_배너_아이폰 가격확인`(ip_pricecheck) · `제작영상_리틀리_1초가격확인`(1sec) · `리틀리_배너_가격확인_아이폰`(ip_pricecheck) — GA4 실측에 ip_pricecheck(30)·1sec(28) 존재 확인.

### (C) 삭제 대상 — 마이그레이션 잔재 7행 (UTM_매핑 시트 18~24행)

`insertColumnBefore` 컬럼 시프트로 생긴 중복 쓰레기(A=페북·B=페북·C=광고그룹명). 대상: 제작영상_리틀리_1초가격확인 / 리틀리_배너_가격확인 / 리틀리_배너_무료폰_260526 / 리틀리_배너_삼성 페스티벌_260610 / 리틀리_배너_지역광고_260611 / 리틀리_배너_가격확인_아이폰 / AI홍보영상_리틀리.
→ 수기 삭제 또는 메뉴 autoDiscover 재실행으로 정리.

---

## 7. 사장님 실행 체크리스트

1. **네이버 수식 패치 배포** (PowerShell, 로컬 PC):
   ```
   cd C:\backup\phonespot_cardnews
   git add apps_script/naver-synce.js
   git commit -m "fix(naver): 기존 행 GA4 수식 통합 UTM_매핑 재작성 함수 추가"
   git push origin main
   ```
   → GitHub Actions가 `clasp push --force` 자동 배포(실패 시 텔레그램 알림).
2. 시트 메뉴 **🟢 네이버 자동화 → 🔄 GA4 수식 전체 재작성** 1회 클릭 → 네이버_통합 GA4 채워지는지 확인.
3. 네이버 슬러그 불일치(region→region_keyword 등) 정리 여부 결정.
4. 미매핑 5건 utm_campaign 확정(위 추천 초안 대조) → UTM_매핑 C열 입력.
5. UTM_매핑 18~24행 삭제.
6. (선택) 광고그룹 추이 06-16·17 반영: 📊 → 🔄 추이 갱신.

## 내가 못 한 것 (정직한 한계)
- `refreshAll()`·각 sync·메뉴 함수는 Google 클라우드 트리거/메뉴 실행 = 이 PC에서 직접 invoke 불가.
- git commit/push는 bash 권한 막힘(`.git/objects` Operation not permitted) → 사장님 PowerShell 필요.
- 네이버 패치 전체파일 `node --check`는 bash 마운트 tearing(24,974B 잘린 사본)으로 불가 → 추가 함수 격리 검증 PASS + 호스트 Read로 진본 무결성 확인으로 대체.

---

## 8. 추가 작업 (2026-06-18, "고치자" 1차) — 코드 D·E 작성 완료 + B 교정표

### D. UTM_매핑 시프트 7행 자동삭제 — 코드 완료 (push 대기)
`apps_script/meta-sync.js`에 `cleanupShiftedUtmRows()` 추가 + 메뉴 `📘 메타 자동화 → 🧹 UTM 시프트 잔재 정리`.
- 동작: B(광고그룹명) 칸에 채널값(페북/네이버/당근/구글/카카오)이 든 행 = 시프트 잔재 signature → 아래→위 삭제(행번호 밀림 방지). 정상 행은 B가 광고그룹명이라 절대 안 걸림. 안전·재사용 가능.

### E. utm 채운 행 상태 ✅ 갱신 — 코드 완료 (push 대기)
`meta-sync.js`에 `flipMappedUtmStatus()` 추가 + 메뉴 `✅ utm 채운 행 상태 갱신`.
- 동작: C(utm_campaign) 채워졌는데 상태가 ⚠️/공백인 정상 행 → `✅ 매핑됨`. 시프트 행(B=채널명)·안내행(※)·utm 빈 행은 제외 → cleanup 전에 돌려도 안전. (대상: ip_pricecheck·1sec 등 3건 + 당근 매핑 4건 상태 보정.)
- `node --check` 격리 PASS. git `M apps_script/meta-sync.js` 확인.

### B. 네이버 슬러그 교정표 (GA4_자동 실측 vs UTM_매핑 C열) — 사장님 수기 교정

| 네이버 광고그룹 | 현재 C열 | GA4 실측 utm_campaign | 조치 |
|---|---|---|---|
| 파워링크_리틀리_지역 | `region` | `region_keyword`(17) | **C열 → region_keyword 로 교정** |
| 파워링크_리틀리_통신사 | `mobile_carrier` | (GA4에 없음) | 확인 — 미집행이거나 utm 다름. powerlink일 가능성 점검 |
| 파워링크_리틀리_갤럭시 | `galaxy` | `galaxy`(91) | 일치 ✅ |
| 파워링크_리틀리_폰스팟 | `phonespot` | `phonespot`(34) | ✅ |
| 파워링크_리틀리_아이폰 | `iphone` | `iphone`(32) | ✅ |
| 파워링크_리틀리_구형기기 | `old_model` | `old_model`(25) | ✅ |
| 파워링크_리틀리_휴대폰성지 | `holyland` | `holyland`(12) | ✅ |
| 파워링크_리틀리_키즈폰 | `kids_phone` | `kids_phone`(11) | ✅ |
| 파워링크_리틀리_효도폰 | `senior_phone` | `senior_phone`(5) | ✅ |
| 파워링크_리틀리_인터넷TV | `internet` | `internet`(5) | 일치 ✅ |
| **(미연결)** | — | **`powerlink`(122, 최다)** | 어느 광고그룹 utm인지 확인 필요(통신사 추정) |

→ 명확 교정 1건(`region`→`region_keyword`). `mobile_carrier`/`powerlink`는 사장님이 네이버 광고 URL의 실제 utm_campaign 확인 후 결정. 교정 후 메뉴 `🟢 네이버 자동화 → 🔄 GA4 수식 전체 재작성` 다시 1회.

> 참고: 당근 `리틀리_가격확인_260610`은 당근→리틀리 링크트리 경유라 GA4 source=`littly`(utm=button/howto/search 등)에 잡혀 광고그룹 직접 바인딩 불명확 → 사장님 확인.

### 통합 push (PowerShell, 로컬 PC) — naver + meta 두 파일
```
cd C:\backup\phonespot_cardnews
git add apps_script/naver-synce.js apps_script/meta-sync.js
git commit -m "fix(ads): 네이버 GA4 전행 재작성 + UTM 시프트정리/상태갱신 함수"
git push origin main
```
→ Actions가 clasp 배포. 이후 시트 메뉴 순서:
1. `🟢 네이버 자동화 → 🔄 GA4 수식 전체 재작성` (A 복구)
2. `📘 메타 자동화 → 🧹 UTM 시프트 잔재 정리` (D)
3. `📘 메타 자동화 → ✅ utm 채운 행 상태 갱신` (E)
4. B 교정표대로 UTM_매핑 C열 수정 → 다시 `🔄 GA4 수식 전체 재작성`
5. 미매핑 5건 utm 확정 입력(6-A 추천초안 대조)
